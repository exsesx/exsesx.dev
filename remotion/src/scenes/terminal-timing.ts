// Shared frame lengths for the terminal phase, so the scenes (Console,
// ProjectsBridge) and the composition (TerminalReel) agree on timing — and
// scenes can animate their own clear/enter relative to their sequence length.
export const CONSOLE_LEN = 250;
export const BRIDGE_LEN = 110;
